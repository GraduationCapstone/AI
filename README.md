# 🎓 PROBE 프로젝트 최종 완성 파일 종합 문서

## 📅 최종 업데이트: 2025-12-30 (한국 시간)


## 1. 프로젝트 개요

### 프로젝트명
**PROBE (Program Repository Operational Behavior Evaluator)**

### 목표
GitHub 저장소 코드를 분석하여 **AI 기반으로 실행 가능성을 판단**하는 시스템

### 핵심 기능
```
GitHub 저장소 URL 입력
    ↓
코드 다운로드 및 분석 (LangChain)
    ↓
벡터 임베딩 생성 (Sentence Transformers)
    ↓
PostgreSQL + pgvector 저장
    ↓
RAG 기반 유사 코드 검색
    ↓
AI 분석 (GPT OSS + Claude API)
    ↓
실행 가능성 판단 결과 반환
```

### 팀 구성
- **Backend**: Spring Boot (Java)
- **AI**: FastAPI (Python) ← **이 프로젝트!**
- **Database**: PostgreSQL + pgvector

### 기간
- **Sprint 1-4**: 4주 개발 (각 1주)
- **현재 상태**: 설계 및 코드 템플릿 완료 (60%)

---

## 2. 최종 아키텍처

### 기술 스택

| 계층 | 기술 | 용도 |
|------|------|------|
| **AI 모델 (이중)** | GPT OSS (Ollama) | 로컬 LLM (무료) |
| | Claude API (Anthropic) | 외부 API (유료, 고품질) |
| **AI 프레임워크** | DSPy 2.4+ | 프롬프트 자동 최적화 |
| **RAG** | LangChain | GitHub 코드 수집 및 RAG |
| **임베딩** | Sentence Transformers | 코드 벡터화 (384차원) |
| **벡터 DB** | PostgreSQL + pgvector | 벡터 유사도 검색 |
| **웹 프레임워크** | FastAPI | RESTful API 서버 |
| **ORM** | SQLAlchemy 2.0 | 데이터베이스 연동 |

### 이중 AI 모델 구조

```
┌─────────────────────────────────────────┐
│         FastAPI Application             │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────────┐   ┌──────────────┐  │
│  │  GPT OSS     │   │  Claude API  │  │
│  │  (Ollama)    │   │  (Anthropic) │  │
│  │              │   │              │  │
│  │ • 로컬 실행   │   │ • 외부 API   │  │
│  │ • 무료        │   │ • 유료       │  │
│  │ • 빠름        │   │ • 고품질     │  │
│  └──────────────┘   └──────────────┘  │
│            ↓               ↓           │
│  ┌──────────────────────────────────┐ │
│  │        DSPy Framework            │ │
│  │    (프롬프트 자동 생성)           │ │
│  └──────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

**선택 가능한 옵션:**
1. `AI_MODEL_MODE=claude` - Claude만 사용 (고품질)
2. `AI_MODEL_MODE=gpt` - GPT OSS만 사용 (무료)

---

## 3. 완성된 파일 목록

### 3.1 설정 파일 (3개)

| 파일 | 경로 | 설명 | 상태 |
|------|------|------|------|
| `.env.example` | `/` | 환경 변수 템플릿 | ✅ 완료 |
| `requirements.txt` | `/` | Python 패키지 목록 | ✅ 완료 |
| `.gitignore` | `/` | Git 제외 파일 | ✅ 완료 |

### 3.2 설정 모듈 (2개)

| 파일 | 경로 | 설명 | 상태 |
|------|------|------|------|
| `__init__.py` | `config/` | 모듈 초기화 | ✅ 완료 |
| `config.py` | `config/` | 이중 모델 설정 관리 | ✅ 완료 |

### 3.3 AI 모델 클라이언트 (4개)

| 파일 | 경로 | 설명 | 상태 |
|------|------|------|------|
| `__init__.py` | `src/gpt/` | GPT 모듈 초기화 | ✅ 완료 |
| `client.py` | `src/gpt/` | GPT OSS 클라이언트 | ✅ 완료 |
| `__init__.py` | `src/claude/` | Claude 모듈 초기화 | ✅ 완료 |
| `client.py` | `src/claude/` | Claude API 클라이언트 | ✅ 완료 |

### 3.4 DSPy 모듈 (4개)

| 파일 | 경로 | 설명 | 상태 |
|------|------|------|------|
| `__init__.py` | `src/dspy_modules/` | DSPy 모듈 통합 | ✅ 완료 |
| `signature.py` | `src/dspy_modules/` | 입출력 Signature 정의 | ✅ 완료 |
| `dspy_modules.py` | `src/dspy_modules/` | ExecutabilityAnalyzer | ✅ 완료 |
| `optimizer.py` | `src/dspy_modules/` | 프롬프트 최적화 (선택) | ✅ 완료 |

### 3.5 임베딩 모듈 (2개)

| 파일 | 경로 | 설명 | 상태 |
|------|------|------|------|
| `__init__.py` | `src/embeddings/` | 임베딩 모듈 초기화 | ✅ 완료 |
| `embedding_generator.py` | `src/embeddings/` | 벡터 생성기 (GPU 지원) | ✅ 완료 |

### 3.6 데이터베이스 모듈 (3개)

| 파일 | 경로 | 설명 | 상태 |
|------|------|------|------|
| `__init__.py` | `src/database/` | DB 모듈 초기화 | ✅ 완료 |
| `models.py` | `src/database/` | SQLAlchemy 모델 (3개 테이블) | ✅ 완료 |
| `db_manager.py` | `src/database/` | CRUD + 벡터 검색 | ✅ 완료 |

### 3.7 LangChain 통합 (3개)

| 파일 | 경로 | 설명 | 상태 |
|------|------|------|------|
| `__init__.py` | `src/langchain_integration/` | 모듈 초기화 | ✅ 완료 |
| `text_splitter.py` | `src/langchain_integration/` | 코드 청킹 | ✅ 완료 |
| `rag_pipeline.py` | `src/langchain_integration/` | RAG 파이프라인 | ✅ 완료 |

### 3.8 API 서버 (1개)

| 파일 | 경로 | 설명 | 상태 |
|------|------|------|------|
| `main.py` | `src/api/` | FastAPI 엔드포인트 | ⏳ 생성 필요 |


## 4. 디렉토리 구조

```
PROBE/
├── .env.example                    ✅ 환경 변수 템플릿
├── .gitignore                      ✅ Git 제외 설정
├── requirements.txt                ✅ Python 패키지
├── main.py                         ⏳ 서버 실행 스크립트
├── init_db.py                      ⏳ DB 초기화 스크립트
├── README.md                       ⏳ 프로젝트 설명
│
├── config/                         ✅ 설정 관리
│   ├── __init__.py
│   └── config.py
│
│
└── src/
    ├── api/                        ⏳ FastAPI 서버
    │   └── main.py
    │
    ├── gpt/                        ✅ GPT OSS (Ollama)
    │   ├── __init__.py
    │   └── client.py
    │
    ├── claude/                     ✅ Claude API
    │   ├── __init__.py
    │   └── client.py
    │
    ├── dspy_modules/               ✅ DSPy 프레임워크
    │   ├── __init__.py
    │   ├── signature.py
    │   ├── dspy_modules.py
    │   └── optimizer.py
    │
    ├── embeddings/                 ✅ 임베딩 생성
    │   ├── __init__.py
    │   └── embedding_generator.py
    │
    ├── database/                   ✅ 데이터베이스
    │   ├── __init__.py
    │   ├── models.py
    │   └── db_manager.py
    │
    └── langchain_integration/      ✅ LangChain RAG
        ├── __init__.py
        ├── text_splitter.py
        └── rag_pipeline.py
```

---
## 데이터 흐름

### 전체 파이프라인

```
1. 사용자 요청
   POST /api/analyze
   {
     "repository_url": "https://github.com/user/repo",
     "requirement": "로그인 기능 테스트"
   }
   
   ↓

2. GitHub 코드 다운로드 (LangChain GitHub Loader)
   → 50개 파일 다운로드
   
   ↓

3. 코드 청킹 (CodeChunker)
   → 200개 청크 생성
   
   ↓

4. 벡터 임베딩 (EmbeddingGenerator)
   → 각 청크를 384차원 벡터로 변환
   
   ↓

5. 벡터 DB 저장 (PostgreSQL + pgvector)
   → HNSW 인덱스로 빠른 검색
   
   ↓

6. RAG 검색 (RAGPipeline)
   → 요구사항과 유사한 코드 5개 검색
   
   ↓

7. AI 분석 (DSPy + GPT/Claude)
   Mode 1: GPT만 사용 (빠름, 무료)
   Mode 2: Claude만 사용 (느림, 고품질)
   Mode 3: GPT → Claude (하이브리드)
   
   ↓

8. 결과 저장 (analysis_results 테이블)
   
   ↓

9. 응답 반환
   {
     "is_executable": true,
     "reasoning": "...",
     "confidence_score": 85
   }
```


## 🔗 참고 링크

| 항목 | URL |
|------|-----|
| **GitHub 레포** | https://github.com/GraduationCapstone/AI |
| **Ollama** | https://ollama.com/ |
| **Claude API** | https://console.anthropic.com/ |
| **DSPy** | https://github.com/stanfordnlp/dspy |
| **pgvector** | https://github.com/pgvector/pgvector |
| **LangChain** | https://python.langchain.com/ |
| **FastAPI** | https://fastapi.tiangolo.com/ |


_이 문서는 PROBE 프로젝트의 현재 상태를 정확히 반영합니다._
